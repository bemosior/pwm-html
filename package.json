{
  "name": "pwm-html",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "test": "node --test test/*.test.js",
    "test:browser": "npx playwright test"
  },
  "dependencies": {
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
